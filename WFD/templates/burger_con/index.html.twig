{% extends 'base.html.twig' %}

{% block title %}Burger index{% endblock %}

{% block body %}

    <h1>Burger index</h1>
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Brand</th>
                <th>Flavour</th>
                <th>Summary</th>
                <th>Price</th>
                <th>Description</th>
                <th>Ingredients</th>
                <th>Photo</th>
                <th>Public</th>
                <th>Creator</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for burger in burgers %}
            <tr>
                <td>{{ burger.id }}</td>
                <td>{{ burger.name }}</td>
                <td>{{ burger.summary }}</td>
                <td>{{ burger.price }}</td>
                <td>{{ burger.description }}</td>
                <td>{{ burger.ingredients }}</td>
                <td>{{ burger.photo }}</td>
                <td>{{ burger.public ? 'Yes' : 'No' }}</td>
                <td>{{ burger.user.username }}</td>
                <td>
                    <a href="{{ path('ice_cream_show', {'id': burger.id}) }}">show</a>
                    <a href="{{ path('ice_cream_edit', {'id': burger.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('burger_con_new') }}">Create new</a>

{% endblock %}